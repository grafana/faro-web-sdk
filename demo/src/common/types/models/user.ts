export type UserModel = {
  email: string;
  id: string;
  name: string;
  password: string;
};

export type UserPublicModel = {
  email: string;
  id: string;
  name: string;
};

export type UserInputModel = {
  email: string;
  name: string;
  password: string;
};
