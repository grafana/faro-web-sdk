---
kind: pipeline
name: grafana-javascript-agent

steps:
- name: Install dependencies
  image: node:lts-stretch
  commands:
  - yarn install --frozen-lockfile

- name: Build
  image: node:lts-stretch
  commands:
  - yarn build

- name: Lint
  image: node:lts-stretch
  commands:
  - yarn quality

- name: Publish
  image: node:lts-stretch
  commands:
  - echo //registry.npmjs.org/:_authToken=${NPM_TOKEN} > .npmrc
  - yarn run publish from-git
  environment:
    NPM_TOKEN:
      from_secret: NPM_TOKEN
  when:
    event:
    - tag
---
kind: signature
hmac: 5a8101aa8620130d6cd883688ac328adf6c25cb7bdbc60f5c51bfcf1099390fc

...
