---
kind: pipeline
name: grafana-javascript-agent

steps:
- name: Install dependencies
  image: node:lts-stretch
  commands:
  - yarn install --frozen-lockfile

- name: Build
  image: node:lts-stretch
  commands:
  - yarn build

- name: Lint
  image: node:lts-stretch
  commands:
  - yarn quality

- name: Publish
  image: node:lts-stretch
  commands:
  - git fetch --tags
  - echo //registry.npmjs.org/:_authToken=${NPM_TOKEN} > .npmrc
  - yarn run publish from-git --yes --no-verify-access
  environment:
    NPM_TOKEN:
      from_secret: NPM_TOKEN
  when:
    event:
    - tag
---
kind: signature
hmac: 130153697bd7c1febd7069a8c87a0446fd3b16f46d810173db3e093d76cd9c5d

...
