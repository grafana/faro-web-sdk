---
kind: pipeline
name: grafana-javascript-agent

steps:
- name: Install dependencies
  image: node:lts-stretch
  commands:
  - yarn install --frozen-lockfile

- name: Build
  image: node:lts-stretch
  commands:
  - yarn build

- name: Lint
  image: node:lts-stretch
  commands:
  - yarn quality

- name: E2E
  image: cypress/included:10.0.3
  commands:
  - yarn ci:e2e

- name: Publish
  image: node:lts-stretch
  commands:
  - git fetch --tags
  - echo "//registry.npmjs.org/:_authToken=$NPM_TOKEN" > .npmrc
  - yarn run publish from-git --yes --no-verify-access
  environment:
    NPM_TOKEN:
      from_secret: NPM_TOKEN
  when:
    event:
    - tag
---
kind: signature
hmac: 197925165f6c379883b0103c6eb463ed7cc238cac716477b9f7424e43398a54e

...
