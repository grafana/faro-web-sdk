apiVersion: 1

providers:
  - name: 'grafanaAlloy'
    orgId: 1
    folder: 'Grafana Alloy'
    folderUid: ''
    type: 'file'
    disableDeletion: true
    editable: true
    updateIntervalSeconds: 10
    allowUiUpdates: false
    options:
      path: '${GRAFANA_DASHBOARDS_PATH}'
