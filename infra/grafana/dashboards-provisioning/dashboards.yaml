apiVersion: 1

providers:
  - name: 'grafanaAgent'
    orgId: 1
    folder: 'Grafana Agent'
    folderUid: ''
    type: 'file'
    disableDeletion: true
    editable: true
    updateIntervalSeconds: 10
    allowUiUpdates: false
    options:
      path: '${GRAFANA_DASHBOARDS_PATH}'
